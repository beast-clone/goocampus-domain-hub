/* Sv editor bar — stubbed out for the self-hosted mirror.
   The original imported (removed) and
   (removed)*.mjs at runtime. Those are Sv
   infrastructure and have no purpose on a published copy, so this keeps the
   export surface script_main expects and returns an inert component. */
export function createEditorBar() {
  return function EditorBar() { return null; };
}
export default { createEditorBar };
